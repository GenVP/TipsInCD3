
&После("ВыполнитьЗагрузкуМетаданных")
Процедура КД3_ВыполнитьЗагрузкуМетаданных(ОбщиеПеременные, АдресВременногоХранилища)
	
	Конфигурация = Неопределено;
	Если ОбщиеПеременные.Свойство("Конфигурация", Конфигурация) И ЗначениеЗаполнено(Конфигурация) Тогда
		КД3_Метаданные.ЗагрузитьОписаниеМетаданных(Конфигурация, Неопределено);
	КонецЕсли;
	
КонецПроцедуры
